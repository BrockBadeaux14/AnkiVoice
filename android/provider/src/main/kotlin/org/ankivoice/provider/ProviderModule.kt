package org.ankivoice.provider

/**
 * Skeleton for provider access. AV-022 gives this module the app's only network
 * route, the Keystore credential store, the quota ledger and diagnostics (#17, #18).
 * It makes no platform or network calls yet.
 */
internal object ProviderModule
