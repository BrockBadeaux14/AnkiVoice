package org.ankivoice.ankidroid

/**
 * Skeleton for the AnkiDroid adapter. AV-022 gives this module every ContentResolver
 * call: the access preflight (#24), the card provider and review transport (#25) and
 * VoiceQA provisioning (#10). It makes no platform calls yet.
 */
internal object AnkiDroidModule
